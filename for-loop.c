#include <stdio.h>
 
int main () {

   int counter;
	
   /* for loop execution */
   for( counter = 10; counter < 20; counter = counter + 1 ){
      printf("value of counter: %d\n", counter);
   }
 
   return 0;
}
