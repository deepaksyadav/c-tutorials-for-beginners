#include <stdio.h>
 
int main () {

   /* local variable definition */
   int intVar = 10;

   /* while loop execution */
   while( a < 20 ) {
      printf("value of intVar: %d\n", intVar);
      intVar++;
   }
 
   return 0;
}
